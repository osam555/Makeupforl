/**
 * 같은 사진이 두 번 올라가 있는 후기.
 *
 * 후기 원문을 옮기다가 두 쌍이 겹친 것을 발견했다. 파일 이름은 다른데 내용은
 * 바이트까지 같다(sha256 으로 확인). 예전에 같은 사진을 두 번 올린 것으로 보인다.
 *
 *   r15 = r25 · r14 = r24  (날짜·제목도 동일)
 *
 * 지우지 않고 가리기만 한다. Firestore 문서를 지우면 되돌릴 수 없고, 무엇을 왜
 * 지웠는지도 남지 않는다. 뒤 번호를 남긴 것은 목록이 최근 것부터 내려오는데
 * 앞 번호가 아래쪽에 한 번 더 나와서다.
 *
 * 이 파일을 따로 둔 이유: 목록은 서버(reviewImages.ts)와 브라우저(ReviewsList)
 * 양쪽이 함께 봐야 하는데, reviewImages 는 firebase-admin 을 끌어온다.
 * 클라이언트에서 그 파일을 import 하면 서버 SDK 가 브라우저 번들로 딸려 들어가
 * 빌드가 깨진다. 그래서 아무것도 끌어오지 않는 이 파일에 값만 둔다.
 *
 * 나중에 관리 화면에서 정리하면 이 파일은 지워도 된다.
 */
export const DUPLICATE_REVIEWS = new Set(['r15', 'r14'])
